export default {
  clearMocks: true,
  collectCoverage: true,
  coverageDirectory: "coverage",
  coverageProvider: "v8",
  transform: {},
  verbose: false,
  preset: "ts-jest",
  testEnvironment: "node"
};
